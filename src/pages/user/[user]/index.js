import React from 'react'

function Index() {
    return (
        <div>This is the root page, for [user] folder, if after reaching till [user] folder , we not mention any file </div>
    )
}

export default Index